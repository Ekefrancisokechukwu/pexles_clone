// import { useParams } from "react-router";
// import { useFetchSingle } from "../utils/useFetch";

const DetailPage = () => {
  // const { id } = useParams();
  // const { data } = useFetchSingle(id);
  return <div>DetailPage</div>;
};
export default DetailPage;
